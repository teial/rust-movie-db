[package]
name = "api-lib"
version = "0.1.0"
edition = "2021"

[dependencies]
axum = "0.7.4"
tracing = "0.1.40"
sqlx = { version = "0.7.4", features = [
    "tls-native-tls",
    "macros",
    "postgres",
    "uuid",
    "chrono",
    "json",
] }
